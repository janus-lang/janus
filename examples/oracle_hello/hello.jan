// SPDX-License-Identifier: LUL-1.0
// Copyright (c) 2026 Self Sovereign Society Foundation

// Oracle Proof Pack Demo - hello.jan
// This demonstrates perfect incremental compilation with measurable results

import std.io

func greet(name: string) -> string {
    return "Hello, " + name + "!"
}

func main() {
    let message = greet("Janus")
    print(message)

    // This is a comment that should not trigger rebuilds
    let version = "v0.1.0"
    print("Version: " + version)
}
