// SPDX-License-Identifier: LUL-1.0
// Copyright (c) 2026 Self Sovereign Society Foundation

func sum(a: i32, b: i32) -> i32 {
    let result: i32 = a + b;
    return result;
}

func main() {
    let x: i32 = 10;
    let y: i32 = 20;

    let ptr: *i32 = 0;
    
    // Arrays not fully supported yet in lowering, just testing parser
    // let arr: [5]i32 = [1, 2, 3, 4, 5]; 

    let s = sum(x, y);
    print(s);
}
